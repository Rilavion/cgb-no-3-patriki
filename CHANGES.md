# Что изменено (11.08.2026)

Заменить нужно эти файлы на хостинге: **index.html, info.html, news.html, faq.html, main.js, style.css, home-news.js**.
Свои `logo.png` и `fon.png` НЕ перезаписывайте — в архиве их нет, остаются ваши текущие.

## Раунд 2 (по вашим правкам)

### 1. Меню снова сбоку — на всех экранах
- Убраны горизонтальные «пилюли» из шапки: в шапке остались только логотип,
  поиск, тема, «Войти» и кнопка-бургер.
- Боковая выезжающая панель (drawer) теперь работает и на десктопе, и на телефоне:
  карточки разделов с цветными иконками, подписи, шапка с логотипом,
  подвал «Приёмное отделение · 24/7», полупрозрачный блюр фона.
- JS упрощён: nav сразу переносится в `<body>` (backdrop-filter шапки ломал position:fixed),
  убрано переключение десктоп/мобайл — режимов больше нет, панель одна.

### 2. Колокольчик убран
- Кнопка уведомлений удалена из шапки страниц info.html, news.html, faq.html.
- `notify-engine.js` НЕ удалён: он продолжает показывать всплывающие тосты
  о новых публикациях. Если тосты тоже не нужны — просто удалите строку
  `<script src="notify-engine.js...">` из всех страниц.

### 3. Новости на главной: картинки + заполнение ленты
- В карточки новостей на главной добавлено изображение публикации
  (поле `image` из таблицы `news`, либо первое из `images`; берётся через `CGB_NEWS.firstImage` — то же, что на странице новостей).
- Лента всегда заполнена в ширину, без пустот и без горизонтальной прокрутки:
  - **1 новость** — карточка на всю ширину (картинка крупнее, виден короткий текст);
  - **2 новости** — каждая ровно 50% ширины;
  - **3 новости** — по трети ширины;
  - **больше 3** — показываются только 3 самые свежие, остальные доступны
    по ссылке «Все новости» (лимит `MAX_ITEMS=3` в home-news.js).
- На телефоне (≤900px) карточки становятся горизонтальной лентой со свайпом —
  иначе три карточки в ряд были бы слишком узкими.

## Раунд 1 (прежние правки, сохранены)

### Новости на главной (исправлено)
- Причина бага: на index.html не было кода обращения к Supabase за новостями.
- `home-news.js` тянет последние публикации из `news` через `CGB_NEWS` (news.js).
- Клик по карточке открывает читатель статьи прямо на главной (news-reader.js).

### Заголовок «Центральная Городская Больница №3» (исправлено)
- Убран `filter: drop-shadow(...)` у градиентного текста (он в связке с
  `background-clip: text` обрезал глифы в Safari/части браузеров).
- `line-height` .98 → 1.08 + защитные отступы у строк: «у», «р», «№» не срезаются.

### «О больнице» на главной
- Убрано перечисление «12 отделений, 180+ специалистов, собственная лаборатория и
  служба скорой помощи».

### Прочее
- Исправлено наезжание нижней подписи (©) на блок новостей при высоте окна ≤768px.
- Все `?v=` в ссылках на css/js подняты до `3` — у посетителей сбросится старый кэш.
